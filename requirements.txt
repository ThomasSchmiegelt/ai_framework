fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0
ddgs>=0.1.0
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=1.0.0
Pillow>=10.3.0
python-multipart>=0.0.9
aiofiles>=23.2.0
aiosqlite>=0.20.0

# Engineering-Tools (Berechnung, Einheiten, Gleichungslöser, Diagramme)
# matplotlib dient zusätzlich dem PDF-Export (Dokumente/Präsentationen) ohne LaTeX.
numpy>=1.26.0
scipy>=1.13.0
sympy>=1.12
Pint>=0.23
matplotlib>=3.8.0

# PDF-Export von Dokumenten: ReportLab (BSD, MIT-kompatibel) für echten Fließsatz
# mit Auto-Spaltenbreite/Umbruch/Seitenumbruch. Formeln werden via matplotlib-mathtext
# als PNG eingebettet, Mermaid-Diagramme als PNG aus dem Browser. Präsentations-PDFs
# laufen weiterhin über matplotlib.
reportlab>=4.1.0

# Mail (IMAP/POP3, tools/mail.py) nutzt ausschließlich die Python-Standardbibliothek
# (imaplib, poplib, email) — daher kein zusätzliches Paket nötig.

# Postfach-Auswertung (Tab „📮 Postfach", tools/mailstore.py):
#  - .mbox/.eml über die stdlib (mailbox/email) — kein Paket nötig.
#  - .msg über extract-msg (BSD, MIT-kompatibel).
extract-msg>=0.48
#  - .pst: KEIN Zusatzpaket nötig — der eingebaute Reiner-Python-Leser tools/pst_pure.py
#    (MIT, nur stdlib) liest Unicode-PST direkt. Reihenfolge der genutzten Reader:
#      1. WINDOWS (optimal, falls vorhanden): installiertes Outlook per COM (pywin32,
#         permissiv). Liest alle Ordner/Anhänge nativ. Bei Bedarf: pip install pywin32
#      2. optional: libpff-python (LGPL, schwaches Copyleft) — unverändertes, dynamisch
#         gezogenes Paket, bewusst zugelassene Ausnahme NUR für PST. Baut auf Windows selten
#         per pip; Import in mailstore.py ist weich. Bei Bedarf: pip install libpff-python
#      3. Fallback (immer verfügbar): tools/pst_pure.py — reiner Python, keine Fremdlib.
# pywin32>=306        ; sys_platform == "win32"
# libpff-python>=20231205

# RAG (Wissenssammlungen): benötigt KEINE zusätzlichen pip-Pakete — die
# Vektorsuche nutzt NumPy (oben), HTTP-Aufrufe httpx (oben) und die
# Dokumentextraktion pypdf/python-docx/openpyxl (oben). Die Embeddings laufen
# über ein kleines Ollama-Modell (config.json: "embed_model", Standard
# nomic-embed-text) — einmalig ziehen mit:  ollama pull nomic-embed-text
