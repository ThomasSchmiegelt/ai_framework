# CLAUDE.md

Guidance for Claude Code (claude.ai/code) when working in this repository.

> **Full detail lives in the docs — read them before deep work:**
> developer reference `docs/ENTWICKLUNG.md`, end-user manual `BEDIENUNGSANLEITUNG.md`.
> This file is the lean quick-reference: project rules, conventions, and a map of where
> each subsystem is documented. Keep it under the 40k-char limit — put new deep detail
> in `docs/ENTWICKLUNG.md`, not here.

## Project Constraints (must follow)

- **Cross-platform:** runs on **Windows and Linux**. The Python/JS core is platform-neutral — keep it that way: no OS-specific paths, no platform-only dependencies. Guard platform-specific behavior (e.g. systemd) with a runtime check (`sys.platform`). Both `.bat`/`.ps1` (Windows) and `.sh` (Linux) scripts coexist; never remove one set.
- **MIT / open source:** all added code and dependencies must be MIT-compatible. Do **not** introduce AGPL/GPL/copyleft dependencies. **PST reading (three-tier, all permissive by default):** the Postfach tab reads `.pst` in this order — (1) installed **Outlook via COM (`pywin32`, permissive)** on Windows, (2) optional `libpff-python`, (3) a **built-in pure-Python reader `tools/pst_pure.py` (MIT, no third-party lib)** that implements the [MS-PST] NDB+LTP layers itself (Unicode PST, permute decrypt, PidTag* extraction, attachments, PST-CRC password verification). Because of tier 3, `.pst` is **always** readable with zero non-permissive deps. **Documented exception:** `libpff-python` (LGPL, weak copyleft) is allowed *only* as an unmodified, dynamically pulled, **optional** dependency (soft `try/except`). Do not extend copyleft usage beyond this. Note: PST passwords are only a CRC checksum, not encryption — content is readable regardless; the entered password is merely verified against the stored CRC. **Second documented exception (Transkription):** `faster-whisper` (MIT) pulls **PyAV** (BSD) as its audio decoder, and PyAV's wheels bundle **ffmpeg libraries (LGPL, weak copyleft, dynamically linked, unmodified)** — needed to decode compressed uploads (mp3/m4a/webm-opus). This is allowed on the same basis as the `libpff` exception (unmodified, dynamically pulled weak-copyleft), and — unlike `libpff` — it is a **required** dependency because the local STT engine was chosen as mandatory. No GPL-enabled ffmpeg components are used. Do not extend copyleft usage beyond these two documented cases.

## What This Project Is

AI_Framework_Thomas is a German-language, **general-purpose** AI chat interface that runs entirely locally: local Ollama LLMs wrapped in a **FastAPI** backend with a **vanilla-JS** frontend, plus a tool-calling agentic loop, SQLite conversation persistence, and productivity tools (engineering calculators, material lookup, unit conversion, SymPy solver, matplotlib charting, PDF/DOCX/PPTX/LaTeX generation, planner/CPM, research, RAG).

Derived from IG-11 as the general variant. The UI has **24 tabs** (Chat, Canvas, Agenten, Recherche, RAG, Dokumente, Medizin, Mathe, Mail 🚧, Planer, Matrix, Anfrage, Code, Verzeichnis-Analyse, Postfach, Morph-Kasten, Jury, Logs, Patente, Rechnungen, Arbeitszeugnisse, Varianten, To-Do, Transkription — `data-tab` values in `static/index.html`) and **seven modes** (`maschinenbau`, `ki`, `soziales`, `marketing`, `finanz`, `geschaeftsfuehrung`, plus a user-configurable `custom`) that drive the color scheme and an optional domain framing. See `docs/ENTWICKLUNG.md` for the mode/system-prompt machinery (`_augment_prefix`, `pure_llm`, `lang`).

## Running the App

**Linux (this machine):**
```bash
source venv/bin/activate
uvicorn main:app --host 127.0.0.1 --port 8780 --reload   # dev
uvicorn main:app --host 0.0.0.0  --port 8780 --reload    # server mode
```

**Windows:**
```powershell
.\venv\Scripts\Activate.ps1
uvicorn main:app --host 127.0.0.1 --port 8780 --reload
```
Or use the scripts: `start.bat` / `start_server.bat` (Windows), `start.sh` (Linux, root of repo — supports `AI_HOST`/`AI_PORT`/`AI_SSL_CERT`/`AI_SSL_KEY` env vars).

Ollama must run separately on `http://localhost:11434` with at least one `config.json` model pulled. For RAG, pull the embedding model once: `ollama pull nomic-embed-text`. Browser: <http://localhost:8780>.

## Testing / Diagnostics

There is **no automated test suite or linter configured** (no pytest, no ruff/flake8 config) — verify changes by running the app and exercising the affected tab/endpoint manually. Two manual smoke-test scripts exist for backend-only checks (both auto-detect dev vs. portable-bundle layout):
- `python3 test_chat.py [appdir]` — hits `/api/chat` directly (bypassing the frontend), once without tools and once with tools, printing raw SSE frames. Requires the app already running.
- `diagnose.bat` (Windows) — collects OS/Python/package/port/Ollama status into `diagnose_report.txt`.

## Key Configuration

`config.json` controls allowed models, default model, `embed_model` (RAG embeddings, default `nomic-embed-text`), Ollama URL, port/host, and installer flags (`hidden_tabs_default`, `enable_api`, `allow_python_exec`). **No environment variables** — everything is read from this file at startup. `allow_python_exec` (default `true`) gates server-side Python execution in the Code tab (`POST /api/code/run-python`); `make_server` defaults it to `false` (runs arbitrary code on the host). `seed_todo_demo` (default `false`, **`true` in this repo**) imports the large fictional To-Do demo (`defaults/todo_demo.json`) once at startup via `_seed_todo_demo()` (marker `data/todo/.demo_seeded`).

## Architecture (sketch)

```
Frontend (Vanilla JS, 24 tabs)   static/js/app.js, chat.js, canvas.js, …
       ↓ SSE streaming
Backend (FastAPI, async)         main.py  (~14,900 lines, single file)
       ↓ tools/llm.py (unified)
Ollama (local)  ──or──  external OpenAI-compatible API (OpenRouter, OpenAI, Groq …)
       ↓
SQLite (aiosqlite)               data/ai_framework_thomas.db  — schema in db.py
```

Async throughout (httpx, aiofiles, aiosqlite). **All LLM calls go through `tools/llm.py`** (`_llm.chat` / `_llm.stream`), which transparently routes to local Ollama or an external OpenAI-compatible provider based on the model name (`"<provider_id>::<model>"` prefix = remote). `/api/chat` runs an agentic loop (max 8 iterations) and streams SSE frames (`text`, `canvas`, `image`, `map`, `tool_start`, `tool_done`, `rag`, `adaptive`, `error`, `done`).

## Development Conventions (must follow)

These prevent the most common backend mistakes:

- **API endpoint ordering:** register all `@app.<method>` routes **before** the `app.mount("/", StaticFiles(...))` call at the bottom of `main.py`. The static mount is a catch-all — routes after it are unreachable.
- **VRAM guard (~6 GB, one local model at a time):** every Ollama call **must** be wrapped `async with _model_session(model), httpx.AsyncClient() as client:` (using `_llm.chat`/`_llm.stream` inside). `_model_session` unloads the previous model on switch and serializes generations via `_model_lock`. It is a **no-op for remote models**. Never call Ollama outside it.
- **Model validation:** always pass user-supplied model names through `_pick_model(body.get("model"), fallback)`. Rejects placeholders (`Lade…`, `qwen3.6-16k:latest`), falls back to `fallback or DEFAULT_MODEL` (`ministral-3:3b`).
- **Local-only enforcement:** for features that default to never calling a remote API (Verzeichnis-Analyse, Postfach; „Web-Recherche lokal" profile flag `research_local_only` — gilt für Recherche-Tab, Matrix, `/such`, Deepdive-mit-Websuche und Patent-Analyse, gemeinsamer Helfer `await _research_model(preferred, fallback)`; **zusätzlich unabhängig vom Schalter** ein reiner Rückfall: scheitert ein API-Modell bei der Recherche, wiederholt `_research_llm_json` den Aufruf einmal lokal (`_research_fallback_model`) und meldet das per `notice`-Frame), use `await _analysis_model(preferred)` (Verzeichnis/Postfach) bzw. `await _local_model(preferred)` — beide coercen zu einem installierten lokalen Modell und liefern `None` ohne lokales LLM (→ HTTP 503 / SSE-error). **Ausnahme per Profil:** `confidential_allow_api` (Checkbox „API-Modelle für vertrauliche Auswertungen", Standard aus) — nur dann UND nur bei explizit gewähltem Remote-Modell nutzt `_analysis_model` das API-Modell. Embedding-Funktionen (pst/similarity, to-rag, ask-Retrieval) brauchen weiterhin lokales Ollama. Remote detection: `_llm.is_remote(model)` (name contains `::`); availability: `_local_llm_available()`. **Globaler Geheim-/Lokal-Modus:** Profil-Flag `local_only_mode` (Getter `_secret_local()`, Sidebar-Schnellschalter `#btn-secret-mode` + Profil-Checkbox `#profile-local-only`) coerct **zentral** an den zwei Chokepoints `_model_for` (Remote-Rollenzuweisung → `DEFAULT_MODEL`) und `_pick_model` (Remote-Request → lokaler Fallback) — deckt fast alle Endpoints ab, ohne sie einzeln anzufassen; zusätzlich impliziert es `_research_local_only()` und übersteuert `confidential_allow_api` (→ `False`). Beidseitig schaltbar. Transkription erzwingt dann `engine=local`.
- **Erweiterte Chat-Werkzeuge (Code-Interpreter + Recherche):** Profil-Flag `chat_code_interpreter` (Getter `_chat_agent_tools()`, Checkbox `#profile-chat-agent`, **Standard aus** — kleine Modelle sind mit dem Werkzeug-Loop überfordert). Wenn an, ergänzt `_chat_generator` `active_tools` um das **gated** Tool `_RUN_PYTHON_TOOL_DEF` (`run_python`, NICHT in `TOOL_DEFS` → nur so angeboten; nur bei `ALLOW_PYTHON_EXEC`) + bietet `web_search` autonom an (falls `_web_search_allowed()`); `_execute_tool("run_python")` = Sandbox `_run_python_code` (JSON `{text,images}`), der Loop streamt `images` als `image`-Frames und gibt dem Modell nur `text`. Systemprompt-Hinweis wird angehängt. Token unverändert im `done`-Frame.
- **Chat-Antwortqualität (`_chat_generator`):** (a) **Mathe→Code** — bei `_looks_like_math(user)` weist ein Systemprompt-Zusatz an, die Aufgabe **vorzugsweise per Code** (`run_python` bzw. immer verfügbares `calculate`) zu lösen; (b) **Fakten→Quelle** — ist `web_search` in `active_tools`, verlangt `_web_hint` konkrete/überprüfbare Angaben (PS, Baujahre, Preise, Namen) per Websuche zu belegen, und `_BASE_GUARD` fordert für unbelegte Detailzahlen die Kennzeichnung als unsicher; (c) **Rettungsantwort** — endet der Werkzeug-Loop ohne sichtbaren Text (Reasoning-Modell steckt alles ins Denken, oder Max-Iterationen bei tiefer Recherche), erzwingt `_force_answer(messages, model, num_ctx)` EINEN finalen Aufruf ohne Werkzeuge/Denken aus dem gesammelten Kontext. Frontend: sichtbare **„arbeitet…"-Anzeige** (`makeWorking()`, Sanduhr+Spinner) in Chat & Bilderstellung bis Inhalt kommt.
- **Parsing LLM JSON:** small models wrap JSON in `<think>` tags and code fences. Strip them, extract JSON via regex, always provide a fallback — see `_parse_llm_json` / `_derive_adaptive_prompt` / planner endpoints for the pattern.
- **New frontend modules:** add `<script src="/static/js/mymodule.js">` to `static/index.html` **and** call `MyModule.init()` in the `DOMContentLoaded` handler in `app.js`.

## Subsystem Map

Each subsystem's deep documentation is in `docs/ENTWICKLUNG.md` (section numbers below). The lines here are orientation only.

| Subsystem | Key files | Detail |
|---|---|---|
| Agentic loop & SSE | `main.py` `_chat_generator` | ENTWICKLUNG §2.2 |
| VRAM guard & model roles | `main.py` `_model_session`, `_model_for` | §2.3 |
| Modes / system-prompt vorspann | `main.py` `_augment_prefix` | §11 |
| LLM abstraction & API providers | `tools/llm.py`, profile modal | §12 |
| RAG engine | `tools/rag.py`, `rag.js`, `db.py`. **Bereinigung zweistufig** (`clean_text(text, level)`, Spalte `rag_collections.clean_level`): `standard` = verlustfrei (NFKC, typografische Zeichen → ASCII, unsichtbare Steuer-/Formatzeichen, Silbentrennung, Seitenzahlen/`12/144`); `strikt` = zusätzlich Markdown, `[Text](URL)` → Text, nackte URLs und wiederkehrende Kopf-/Fußzeilen (`_drop_boilerplate`: Häufigkeit **+** gleichmäßige Zeilenabstände **+** Kürze — rein statistisch, damit Inhaltssätze nicht mitgelöscht werden). Zeichentabelle als `\u`-Escapes. **Embeddingmodell pro Sammlung wählbar** (`embed_model`, lokal oder `anbieter::modell` → `_llm.embed`, OpenAI-`/embeddings`); Sammlungen mit verschiedenen Modellen koexistieren, da `query_collections` je Modell embeddet und abweichende Dimensionen überspringt. Modellwechsel ⇒ Neuindizierung. `GET /api/rag/embed-models` | §13 |
| Agents (favorites, slash, adaptive, legal) | `data/agents/`, `agents.js`, `main.py`; **projekt-gebundene Skill-Agenten** via `project_id` (`GET /api/agents?project_id=`, im globalen Grid ausgeblendet, Kaskaden-Löschen über `DELETE /api/projects/{id}`) | §14 |
| Jury (multi-agent evaluation) | `data/juries/`, `jury.js`, `/api/jury/evaluate`; große Dokumente per Map-Reduce (`_chunk_for_ctx`, `_JURY_CHUNK_SYSTEM`), alle Calls mit `num_ctx`, Tokens im `done`-Frame; Mitglieder-Picker **nach Projekt gruppiert** | §15 |
| Anfrage-Auswertung (RFQ, XLS-Stapel) | `rfq.js`, `/api/rfq/*` (eval, to-plan, ask), eigene Bewertungsspalten (Agent/Prompt, `_sanitize_rfq_columns`), `tools/files.read_table` | — |
| Matrix-Recherche + **Partner-Auswertung** + **Wissensgraph** | `matrix_research.js`, Zellen über `/api/chat` (`science`+`use_tools`); Spalten mit `active`/`ctx` (Schalter „ausführen"/„Kontext", `_runColumn` = ▶ Spalte, Kontext-Kaskade über vorherige Spalten). **Wissensgraph** (`🕸 Graph`, vertikaler Splitter `#matrix-split`/`#matrix-splitter`, Cytoscape.js vendored in `static/js/cytoscape.min.js`): Knoten = Zeilen (stabile `_gid` je Zeile, überlebt Umsortieren), Kanten = `_graphEdges`/`_graphPos` im selben localStorage-State; Verknüpfung primär über **Merkmal-Knoten (Hubs)**: pro Zeile werden typisierte Merkmale extrahiert (`POST /api/matrix/extract`, `_MATRIX_EXTRACT_SYSTEM`, ein Call je Zeile, Token-Label „Wissensgraph"), je Kategorie (`_attrCats`, Default Tätigkeit/Ort/Tool/Aufgabenbereich/Name, editierbar via `⚙ Kategorien`/`_editCategories`) ein farbiger Hub-Knoten (`_catColor`, `node[kind="hub"]`, Shape `round-tag`); Zeilen mit gleichem Wert hängen am selben Hub (`_hubId` = `attr::<cat>::<val>`, dedup) und sind so verbunden — statt „alle mit allen". `_rowAttrs` (gid→`[{category,value}]`) im localStorage-State; Schalter `#matrix-show-hubs` (`_showHubs`) und `#matrix-shared-only` (`_sharedOnly`, nur von ≥2 Zeilen geteilte Merkmale), Farb-Legende `#matrix-graph-legend`. **Optional zusätzlich** direkte Zeile↔Zeile-Kanten per KI-Vorschlag (`POST /api/matrix/graph`, `_MATRIX_GRAPH_SYSTEM`, Button „✨ Direkt-Kanten"/`_suggestEdges`) **+ manuell** (Verbindungsmodus `_toggleConnect`, Kante anklicken = Label ändern/löschen); manuelle Kanten = `_graphEdges`/`_graphPos`. **Zwei Wege zum Graphen** (gemeinsamer Kern `_tableToGraph` → `_extractAttributes`): Stufe 1 = automatisch im Anschluss an `▶ Alle ausführen` (Schalter `#matrix-auto-graph`, `_autoGraph`), Stufe 2 = Button „🕸 Tabelle → Graph" für eine bereits gefüllte Tabelle (ohne erneute Recherche). Button-Leiste ein-/ausblendbar (`_toggleToolbar`). **Partner-Auswertung** (`_partnerMode`): Firmenliste-Import (`_importCompanyList`, unbegrenzt), Vorlage `_PARTNER_COLUMNS` (Interesse/Profil → Kontaktdaten → LinkedIn/X/Instagram/Facebook/GitHub → Kaltakquise-Mail), Agenten idempotent angelegt via `_ensurePartnerAgents` (`partner_recherche`/`_kontakt`/`_social`/`_mail`, editierbar im Agenten-Tab); Token-Label „Partner-Auswertung". **Zell-Overflow**: `.matrix-cell-result` scrollt (`max-height`), Markdown-Tabellen in Zellen kompakt + horizontal scrollbar (CSS überschreibt die breiten `#matrix-table td`-Regeln); ⤢-Knopf (`_addCellExpand`, bei Hover) öffnet den vollen Zellinhalt im Modal `#matrix-cell-modal` (`_openCellModal`, Esc/Klick-außerhalb schließt) | — |
| Mehrere Ressourcenlisten | `/api/capacity/lists*` + `/api/capacity/selection`, `data/capacity_lists.json` (Migration aus `capacity.json`); `_load_capacity()` = Vereinigung der aktiven Listen | — |
| Token-Zähler | `static/js/tokens.js` (`TokenMeter`), Profil `price_per_1k_in/out`+`currency`; `add(tokens,label)` führt ein **Vorgangs-Protokoll** (Aufschlüsselung pro Vorgangsart + letzte Vorgänge, Klick öffnet `#token-meter-breakdown`); **Vollabdeckung: JEDER `_llm.chat`/`_llm.stream`-Aufruf im Backend meldet `tokens:{in,out}`** (JSON-Antwort bzw. `done`-Frame, Sammel-Dict-Muster `tok={"in":0,"out":0}`); Labels: Chat (inkl. adaptive Ableitung, Komprimieren, Chat→Skill), Deepdive, Suche, Rückfragen, Recherche, Matrix-Recherche, Partner-Auswertung, Wissensgraph, Plan, Plan (Dokument), Plan-Orchestrator, Planer (KI-Chat/Prüfen/Vorschläge/Auto-Struktur/Dossier), Jury, Anfrage(-Frage), Code-Assistent, Code-Projekt, Medizin, Mathe, Morph-Kasten, Verzeichnis-Analyse, Postfach, Patente, Rechnung, Zeugnis, Agenten, Dokumente (Präsentation/Bildanalyse/Verfeinern), Canvas, RAG (optimierter Upload), Mail; **neue LLM-Endpoints müssen `_llm_tok()` melden + Frontend `TokenMeter.add(tokens, label)` aufrufen**; `tools/llm.py` usage-Mapping (Remote-Streaming liefert derzeit keine usage → 0) | — |
| Versionsnummer | `APP_VERSION` (main.py, `_read_version()`): Quelle ist die Datei **`VERSION`** im Programmordner, `config.json` `version` nur noch Rückfall — `VERSION` gehört zum Code und wird vom Updater mitgetauscht, `config.json` bewusst nicht (sonst bliebe die gemeldete Version nach Updates alt). Via `/api/profile` → `app_version`; angezeigt in `#profile-version` + `#sidebar-version` (`profile.js`) | — |
| Softwareverteilung (ACMP) | `update.bat` (Silent `/S`, `/LOG:`, `/NOPIP`, Exit-Codes 0–5, schreibt `VERSION` + `HKLM\SOFTWARE\AI_Framework_Thomas`), `make_acmp.ps1`/`.bat` (Updatepaket aus Positivliste, bricht ab wenn ollama/venv/python/data hineingerieten), `packaging/wix/` (MSI, **ungetestet — nur unter Windows baubar**) | `docs/ACMP.md` |
| Code-Workspace (Code-Tab) | **Ein** chat-getriebener Arbeitsbereich (aider-inspiriert), Modul `code_workspace.js` (`CodeWorkspace`, init in `app.js`) — ersetzt die früheren drei Unter-Tabs IDE/Projekt/JSON (`ide.js`/`code_project.js`/`json_editor.js` werden **nicht mehr geladen**). Zustand: `_files=[{path,content,savedId?}]` + aktive Datei, im localStorage (`code_workspace_v1`). Eine Datei = frühere IDE, mehrere = Projektstruktur. Prompt oben (`#cw-prompt`) → KI ändert die aktive Datei (`POST /api/code/assist`, Rückfragen via `_renderClarify`, Token „Code-Assistent") oder erzeugt via „🏗 Struktur" einen Mehrdatei-Baum (`POST /api/code/project`, Token „Code-Projekt"). `▶ Ausführen` = **aktive** Datei je Endung (`_langForPath`/`_detectLang`): `.js/.html`→Canvas-iframe (`_IFRAME_FRAMEWORK`), `.py`→`POST /api/code/run-python`, `.json`→Prüfen/Formatieren statt Run. Dateibaum `_buildTree`/`_renderTree`, Speichern einzelner Dateien via `/api/code` (+`savedId`), Laden `#cw-load`, ZIP-Export `POST /api/code/project-zip`. Beispiele im `⋯`-Menü (`window.EXAMPLE_*`). Auto-Reparatur → `_assist(...,forceCode)`. Rückwärtskompatibel: `window.CodeIDE.loadFromChat` bleibt als Alias (chat.js „▶ Code-Tab"-Button). **Autonomer Coding-Agent (Agent-Harness, „🤖 Agent"):** SSE-Endpoint `POST /api/code/agent` (`_code_agent_generator`) = eigener Werkzeug-Loop (Muster wie der Chat-Tool-Loop) mit `list_files`/`read_file`/`write_file`/`run_python` (Sandkasten, nur bei `ALLOW_PYTHON_EXEC`), arbeitet auf einer In-Memory-Kopie der mitgeschickten Client-Dateien, iteriert bis `max_steps`, streamt `step`/`text`/`files`/`done`; Modell `_pick_model(_model_for("coding"))` (Geheim-/Hartman erzwingt lokal), Tokens `_llm_tok`, Label „Code-Agent". Weiches Fallback: ` ```pfad ``` `-Blöcke aus Text, wenn keine Tool-Aufrufe. Frontend (`code_workspace.js` `_runAgent`/`_applyAgentFiles`/`_agentPreviewAndRepair`): Panel `#cw-agent-*` (Task/Start/⏹ Stopp/↩ Undo/Log), wendet Dateistand an, markiert Änderungen, Auto-Vorschau der Einstiegsdatei; **Canvas-Loop**: `_run()` → `_renderPreview()` rendert vollständige HTML-Dokumente direkt (mit Konsole-Hook `_CONSOLE_HOOK`), reine JS-Snippets im `_IFRAME_FRAMEWORK`; erkannte Konsolenfehler (`_lastErrors`) lösen bis zu 2 Auto-Reparatur-Runden aus. Nur Sandbox-Ausführung, keine Shell. Backend sonst unverändert (`_CODE_PROJECT_SYSTEM`, `_safe_relpath`) | — |
| Dokument-Experte (verallgemeinert) | `/api/agents/from-legal` mit `domain` (Fachgebiet/Rolle), `agents.js` `createLegalAgent` | §14 |
| Kapazität & Zukauf (Planer) | `planner.js` `_openSchedule`/`_capacityAnalysisHtml`, `Planner.openPlan` | — |
| Auto-Strukturieren (Planer) | `planner.js` `_openAutoStructure`/`_applyAutoStructure`, `/api/plans/auto-structure` (LLM-Abhängigkeiten+Phasen, Ressourcen-Entzerrung, zyklensicher) | — |
| Plan-Generierung (Planer) | `planner.js` `_generatePlan` (`/api/plans/generate`) + **`_importDocPlan`** (`/api/plans/from-document`, Button `#btn-plan-from-doc`): Dokument → Ressourcen + Plan, Ziel-Vorgangszahl bis **300**. Gemeinsamer Kern `_generate_plan_core` + RAG-Helfer `_plan_rag_context` | — |
| Chat-Export MD/CSV | `chat.js` `renderMarkdown` (hinterlegt `el._rawMd`): „⬇ .md" je Assistenten-Antwort (`msg-save-bar`), „⬇ CSV" je Markdown-Tabelle (`table-csv-bar`; Semikolon + UTF-8-BOM für Excel); Toolbar `#btn-export-md` = ganzer Chat als .md (`exportChatMd`, app.js), Canvas `#btn-export-csv` = Spreadsheet als CSV (`exportCanvasCsv`) — alles clientseitig ohne Server | — |
| Chat-Feedback (`/-`, `/+`) | `chat.js` `_parseFeedback`/`runFeedback`, `POST`/`GET /api/feedback` (`_append_feedback`, `FEEDBACK_FILE`=`data/feedback.md`): `/- <Text>` protokolliert Fehler/Problem, `/+ <Text>` Idee/Verbesserung als Markdown (nicht ans LLM); im sendMessage-Pfad **vor** Deepdive/Plan/Slash-Agent abgefangen | — |
| 🧭 Assistent-Modus | Profil-Flag `assistant_mode` (Getter `_assistant_mode()`, Sidebar-Schnellschalter `#btn-assistant-mode` + Profil-Checkbox `#profile-assistant-mode`, Standard aus). Frontend: `app.js` `_reflectAssistant`/`window.__reflectAssistant` setzt `<body class="assistant-mode">` → CSS blendet **alle Tabs außer Chat** aus (`body.assistant-mode #tab-bar .tab-btn:not([data-tab="chat"])`) + wechselt auf Chat. Backend: `assistant_mode` impliziert `_chat_agent_tools()` (Code-Interpreter + autonome Websuche) **und** schaltet das Bild-Werkzeug frei — `_GENERATE_IMAGE_TOOL_DEF` `generate_image` wird `active_tools` hinzugefügt (nur wenn `_image_model()` gesetzt), `_execute_tool` ruft den geteilten Kern `_generate_image_core(...)` (aus `/api/image/generate` extrahiert), der Loop streamt das Ergebnis als `image`-Frame. Zusätzlich ein **Intent-Router**-Systemhinweis (`_router_hint`), der die verfügbaren Fähigkeiten auflistet, damit das Modell selbst das passende Werkzeug wählt. Braucht ein fähiges Modell (kleine Modelle → Std-Modus). History speichert wie gewohnt | — |
| Tiefe Recherche (`/recherche`, Auto-Angebot) | `chat.js` `_parseDeepResearch`/`_openResearchForm`/`runDeepResearch` + Auto-Angebot (`_looksLikeResearch`/`_offerResearch`, Profil-Flag `deep_research_offer`, Standard an, im Hartman-Modus aus), SSE `POST /api/deepresearch` (`_deepresearch_generator`): Thema → **automatische Teilaspekte** (LLM-JSON, clamp `depth` 3–12) → je Aspekt `search_with_sources(...,5)` (parallel) → **quellen-gestützte Synthese** mit **Ziel-Wortzahl** (`words`, ~200–4000) und Anti-Halluzinations-Auflage (konkrete Angaben NUR aus Quellen, sonst als unsicher kennzeichnen). Rückfrage-Overlay `#research-ask` (Tiefe- + Umfang-Chips + Fokus, Muster `#bild-help`). Web-Gate `_web_search_allowed()` + `_research_model` (Web-Recherche-lokal/Geheim). Token „Tiefe Recherche" | — |
| Erweiterte Suche (`/such`) | `chat.js` `_parseSearch`/`runSearch` (Aliase `/suche`,`/finde`,`/search`), SSE `/api/search/expand` (`_search_expand_generator`): KI erzeugt **alternative Suchbegriffe** (Synonyme, Fach-/Umgangssprache, engl.), zeigt sie als anklickbare Chips, durchsucht damit das Web (DuckDuckGo, top 4) und fasst mit Quellen zusammen; Token-Label „Suche" | — |
| Dynamische Rückfragen (`/frag`) | Gemeinsames Modul `clarify.js` (`Clarify.ask`), Endpoint `POST /api/clarify` (Fragen mit `type` text/single/multi + `options`); in **Chat** (`runFrag`), **Mathe** & **Medizin** (`_runFrag`) je vor dem Slash-Agent abgefangen. `/frag <Aufgabe>` → Eingabemaske (auch Multiple-Choice) → Antworten werden an die Aufgabe gehängt und über den normalen Pfad des Tabs gesendet; Token-Label „Rückfragen" | — |
| `/plan`-Orchestrator (Chat) | `chat.js` `_parsePlan`/`runPlan`/`_handlePlanEvent`/`_applyPlan`, SSE `/api/plan/strategy` (`_plan_strategy_generator`): Strategie→Agenten→Plan→Jury als **Vorschau**, speichert nichts; Anlegen via vorhandene `/api/agents`+`/api/plans`+`/api/juries`. **Aufgabenzahl konfigurierbar** via `/planN` (`count` 4–60); Belegmaterial aus hinterlegter Datei (RAG) fließt in **Strategie *und* Plan** (`_plan_ground` skaliert mit `count`). Feste Agenten via `/plan … /kürzel` (`_findAgentByToken` → `pinned_agents`, `_plan_pinned_agents`): pinned zuerst, LLM ergänzt nur, beim Anlegen per vorhandener `id` wiederverwendet (kein Duplikat). „Alles anlegen" erstellt zuerst ein **Projekt** und verknüpft Plan/Agenten/Jury (Feld `project_id` in `create_plan`/`create_jury`/`AgentDef`) + ordnet die Unterhaltung zu. Die erzeugten Berater sind **projekt-exklusive Skill-Agenten** (`project_id`, im globalen Grid ausgeblendet, beim Projekt-Löschen kaskadierend entfernt) | — |
| Verzeichnis-Analyse & Morph-Kasten | `dir_analysis.js`/`morph_box.js`, `tools/anonymize.py`. **Verzeichnis-Analyse läuft nur lokal**: `dir_scan`/`dir_analyze_file` erzwingen `_local_model(...)`, ohne lokales LLM → HTTP 503 | §16 |
| Postfach (PST-/Mail-Auswertung) | Tab „📮 Postfach" (`data-tab="postfach"` → `#postfach-panel`), Modul `postfach.js` (`Postfach`), Backend `tools/mailstore.py` + `/api/pst/*`. **Ausschließlich lokal** (Stufe-2-Analyse via `_local_model`, sonst 503). `POST /api/pst/open` liest `.pst` (Outlook-COM/`pywin32` → `libpff`/LGPL → **eingebauter `tools/pst_pure`**, MIT — daher immer lesbar) bzw. `.mbox/.eml/.msg` (stdlib + `extract-msg`, BSD; Passwortfeld). Passwort wird per **CRC** verifiziert (`pst_password_status`, PST-Passwort verschlüsselt nichts — Inhalt trotzdem lesbar), Ergebnis im `password`-Feld der open-Antwort → UI-Hinweis „🔓/⚠". `_pst_reader_name()`/`GET /api/pst/formats` melden den aktiven Reader (`outlook`/`libpff`/`pure`) → Store unter `data/pst/<id>/` (+ Anhänge). Stufe 1 = Absender/Empfänger/Betreff/Datum/Inhalt; **Stufe 2** (`POST /api/pst/analyze`) liest Anhänge (Dokumente via `tools/files.extract`, **Bilder direkt am lokalen Vision-Modell**, kein OCR) + vergibt Themen-Tags (`_PST_TAG_SYSTEM`, Token „Postfach"). **Wissensgraph** (Cytoscape, wie Matrix): Knoten = Mails, **Konnektor-Hubs** = benannte Wortgruppen (`_connectors` im localStorage, editierbar; Beispiel „Lebensversicherung"=Allianz/Continentale/CMI) — eine Mail hängt am Hub, wenn ein Wort in Absender/Betreff/Inhalt/Anhang vorkommt; Klick auf Mail-Blase → volle Mail (`GET /api/pst/{id}/mail/{mid}`). Zusatz: Volltextsuche, Thread-Gruppierung, Zeitleiste, Absender-Cluster + Themen-Tags als Auto-Konnektoren. **Ausbau (alles lokal):** (1) **erweiterte Filter** (Datum/Absender/Anhang-Typ/Tag/Ordner, rein Frontend über `_visibleMails`); (2) **Verwandtschaftsgrad**-Graph in drei Modi (`_graphMode` = Konnektoren·Themen-Nähe·Netz): **Themen-Nähe** = semantische Ähnlichkeit über `POST /api/pst/similarity` (`rag.embed` lokal/CPU → Cosine, `edges` ab 0.5, Cache `data/pst/<id>/similarity.json`), Schwellwert-Slider + Cluster-Färbung (Union-Find); **Kommunikationsnetz** = Personen-Knoten (Absender/Empfänger), Kantenstärke = Korrespondenz (rein Frontend); (3) **In RAG übernehmen** `POST /api/pst/to-rag` (neue/bestehende Sammlung, `ingest_file`) + **„Postfach fragen"** `POST /api/pst/ask` (`query_collections`+`_local_model`, Quellen); (4) **Statistik** (Top-Absender/Monatsvolumen, Frontend), **Near-Duplikate** (Score ≥ 0.97 aus similarity), **Zusammenfassung** `POST /api/pst/summarize` (`_local_model`, Map-Reduce), **CSV/JSON-Export** (Frontend). Auswahl-Scope überall: markiert → gefiltert → alle. **Performance/Bedienung:** Graph rendert **nicht** live, sondern nur per **▶-Button** (`_buildGraph`, `_markStale` markiert veraltet, pulsiert); Liste/Zeitleiste bleiben live. **Konnektor-Schnellfilter** (`#pf-conn-filter`, nur Mails eines Konnektors) + **„nur verbundene"** (`_pruneIsolated`, isolierte Knoten aus). Options-/Chat-Zeilen einklappbar (`#pf-options`/`#pf-chat-wrap`, `_toggleBlock`) → maximiert die Anzeige. **Graph-Befehl** (lokaler Chat) `POST /api/pst/command` (`_PST_COMMAND_SYSTEM`, Postfach-Zeitraum als Kontext) → JSON-Direktive (mode/connector/sender/query/date/has_attachment/only_connected/focus/hops), Frontend `_applyDirective` setzt Steuerelemente + `_focusNeighborhood` (Fokus-Knoten + Nachbarschaft/„Eltern & Kinder" bis hops, Rest abgeblendet). **Speichern/Wieder-Öffnen:** Stores persistieren unter `data/pst/<id>/store.json` (bei `open` erzeugt); `GET /api/pst/stores` listet sie, `GET /api/pst/{id}` öffnet **ohne Neu-Parsen** (liefert Mailliste + gecachte `similarity.json`-Kanten + gespeicherte `settings`), `POST /api/pst/{id}/settings` speichert Ansicht+Konnektoren (Frontend `_openStores`/`_reopen`/`_saveSession`/`_applySettings`, Buttons 📂/💾, Route `stores` VOR `{store_id}`). Alle LLM-Endpoints → **503** ohne lokales LLM. Token-Label „Postfach" | — |
| Mathe (tutor, auto-verify, plotting) | `mathe.js`, `main.py`, `tools/engineering.py` | §2.3, §17 |
| Medizin 2-model pipeline | `medizin.js`, `/api/medizin/*` | §2.3 |
| Backup/restore & branding | `/api/backup` (Schalter `uploads`/`pst`/`secrets`, Standard aus), `/api/restore` (`replace=0` zusammenführen / `1` überschreiben, Pfade über `_safe_relpath`), `data/profile_assets/`. **Beim Anlegen eines neuen Tabs mit eigenem `data/`-Ordner dort einen Eintrag in `_backup_dirs_always()`/`_backup_files_always()` ergänzen** — sonst fehlt der Bereich im Backup. UI: Profil-Modal „Alle Daten sichern & wiederherstellen" (`btn-profile-export`/`btn-profile-import` in `app.js`) | §18 |
| PWA / phone-as-frontend | `static/manifest.json`, `static/sw.js`, `scripts/` | §19 |
| Deployment variants & installers | `install.*`, `make_portable.*`, `make_server.*` | §20 |
| Patente (patent research) | Tab „⚖️ Patente" (`data-tab="patente"`), `patente.js`, `tools/patente.py` + **`tools/epo_ops.py`** (beide reine Logik ohne FastAPI/DB, analog `tools/mailstore.py`); HTTP-Plumbing + Persistenz in `main.py` (`/api/patente/*`). **Datenquellen-Hybrid:** EPO OPS (amtlich: Rechtsstand, INPADOC-Familie, Daten, CPC, Erfinder; OAuth2-Key in `data/epo_ops.json` — gitignored, Backup nur mit `secrets`; Endpoints `/api/patente/ops-config`) primär via `fetch_patent()` (Merge, `source`-Feld), Google-Scraping als Fallback — **gedrosselt** (Lock + Mindestpause + Backoff, Muster `tools/search.py`) und **gecacht** (`data/patente/_cache/`, 30 Tage). Suche mit Boolean/IPC/Datum: OPS-CQL (`build_cql`) bzw. Google-Query (`build_google_query`), Rückgabe `(results, error, source)`. Pipeline (Prüfer-Methodik) = Technik (Prüfschleife) → **Merkmalsanalyse** (`run_merkmalsanalyse`: `extract_claim1` ungekürzt → element-weise Tabelle, bei 2 Dokumenten Claim-Chart-Vergleich, FREIGABE-Schleife) → **Neuheit & erfinderische Tätigkeit** (EPA-Aufgabe-Lösungs-Ansatz, eigene FREIGABE-Schleife; nächstliegender SdT via Projekt-RAG `sdt_kontext`) → Recht (Tabelle je Merkmal) → Umgehung/Innovation/Entwurf/Kritik → Moderator (deterministische `kennzahlen_markdown`-Tabelle + Handlungsempfehlung); Kontextbudget aus `_profile_num_ctx()`. **Stärke-Score deterministisch ohne LLM** (`patent_kennzahlen`: Restlaufzeit/Zitate/Familie/Anspruchsbreite → Triage-Score 0–100, zur Laufzeit in `patente_project_get` angereichert, Score-Spalte sortierbar). **FTO-Produkt-Check** `POST /api/patente/projects/{name}/fto` (`run_fto_check`: Claim-Chart Anspruch 1 ↔ Produktbeschreibung, All-Elements-Rule, Ampel je Merkmal, „keine Rechtsberatung"-Hinweis, gespeichert als Analyse-Typ `FTO_Check`). **📊 Statistik-Subtab** rein Frontend (`_renderStatistik`: Anmelder/Jahre/IPC/Score + White-Space-Matrix). Ergebnisse per RAG indexierbar (`_pat_index_analysis`). **Hinweis:** Google Patents hat keine offizielle API — Scraping birgt ToS-Risiko (aus dem Original-Tool übernommen). Unterliegt wie Recherche der „Web-Recherche lokal"-Option; Modellwahl über Profil-Rolle **`science`**. Analyse/Roadmap: `docs/PATENTE_ANALYSE.md` | not yet in ENTWICKLUNG.md |
| Rechnungen & Angebote | Tab „Rechnungen" (`data-tab="rechnung"`), `rechnung.js`, `tools/dokumente.py` (`invoice_markdown`/`invoice_docx`), `/api/rechnung/*` + `/api/angebot/*`, `/api/firmenprofil`. Beträge (Netto/USt/Brutto, §14-UStG-Pflichtangaben) werden **deterministisch mit `Decimal`** berechnet — **nie** vom LLM — damit Rechnungen rechnerisch korrekt sind; Export PDF (`tools.export.to_pdf`) und DOCX (eigener Positionstabellen-Bauer) | not yet in ENTWICKLUNG.md |
| Arbeitszeugnisse | Tab „Zeugnisse" (`data-tab="zeugnis"`), `zeugnis.js`, `tools/dokumente.py` (`zeugnis_system_prompt`/`zeugnis_user_prompt`), `/api/zeugnis/*`. Text (codierte Zeugnissprache) kommt vom LLM, gerendert mit den generischen Exportern `tools.export.to_pdf`/`to_docx`; Datensätze unter `data/zeugnisse/` | not yet in ENTWICKLUNG.md |
| Variantenvergleich (gewichtete Entscheidung) | Tab „🧮 Varianten" (`data-tab="varianten"`), `varianten.js`, **`tools/decision.py`** (reine Logik: `pairwise_weights` = AHP-Paarvergleich → Gewichte per geom. Mittel + Konsistenz `CR`, `score_variants` = gewichtete Nutzwertsumme/Ranking, `sensitivity`), `/api/varianten/*` (CRUD + `suggest-criteria`/`suggest-variants`/`suggest-pairwise`/`suggest-ratings`/`explain`). Speicher je Vergleich in `data/varianten/<name>/decision.json`; **Gewichte & Ranking rechnet der Server deterministisch beim PUT** (`_var_compute`), nie das LLM (die KI schlägt nur Kriterien/Varianten/Urteile vor). **Bewertungen = Güte** (1–10, 10 = am besten, auch bei Kosten → günstigste=10); `direction` (`benefit`/`cost`) ist **nur Anzeige-Hinweis** („↓"), `score_variants` spiegelt NICHT (sonst würde eine als gut bewertete günstige Variante bestraft). Modellrolle `general`; `suggest-ratings` nutzt `_research_model` (+ optional RAG-Grounding `_plan_rag_context`). **Schnellvergleich (Wischtechnik)** rein Frontend (`varianten.js` `_openSwipe`/`_swipeAnswer`, Overlay `#var-swipe`): iteriert alle Kriterienpaare (i<j), Tasten **←** linke wichtiger (Saaty 3) / **→** rechte wichtiger (1/3) / **↑** gleich (1) bzw. Klick/Touch — schreibt `_data.pairwise` mit Reziprozität, danach `_renderPairwise`+`_save` (Server rechnet Gewichte/CR). **Problem → komplette Tabelle** `POST /api/varianten/auto-fill` (`varianten.js` `_generateAll`, Block `#var-problem`): orchestriert Kriterien→Paarvergleich→Varianten→Bewertungen in EINEM Aufruf (vorhandene `_VAR_*_SYSTEM`-Prompts via `_research_llm_json`, Tokens summiert), optional vorher **Interview** (`Clarify.ask`, Checkbox `#var-gen-interview`) und **Webrecherche** (`search_with_sources`, Checkbox `#var-gen-web`, Default aus — nur der Web-Query ist extern, das LLM bleibt im Geheim-Modus lokal); Varianten werden bewusst nur aus dem Problem (nicht der langen Kriterienliste) erzeugt + einmaliger Minimal-Rückfall, damit kleine Modelle nicht am Prompt-Umfang scheitern. Token-Label „Variantenvergleich" | ENTWICKLUNG §21.1 (Auto-Tabelle/Schnellvergleich) |
| To-Do-Projektbaum mit Wissensgraph | Tab „✅ To-Do" (`data-tab="todo"`), `todo.js`, **`tools/todo.py`** (nur Struktur-Validierung: `new_item`/`sanitize_list`), **Persistenz in SQLite** (`db.py` `todo_*`: `todo_projects`(parent_id-Baum)/`todo_items`(seq)/`todo_edges`/`todo_attachments`(md_text)). **Projektbaum**: Wurzel `id='root'` = Benutzername (`_todo_root_name()` aus Profil), Unterprojekte via `parent_id`; `migrate_todo_json` übernimmt Alt-JSON einmalig (Marker `data/todo/.db_migrated`). Endpoints `/api/todo/*`: `tree`, `projects` (CRUD + `/rename` + `/move` reparent), **`items/{id}/move`+`/reorder`** (Verschieben/Umsortieren), **Anlagen** (`items/{id}/attach` → `tools/files.extract` → `md_text` in DB + Original unter `data/todo_att/<item_id>/`; `attachment/{att_id}` liefert MD bzw. `?orig=1`), **`extract`/`suggest-links`/`next`** (KI), **`search?q=&root=`**, **`graph?root=`**, **`agenda?root=&person=`** und **`ask`** (KI-Daten-Chat) — alle auf den **Teilbaum** `todo_descendants(root)` beschränkt (Wurzel = alles → „aktives Projekt" als Scope). **Daten-Chat** (`POST /api/todo/ask`, Chat-Zeile oben rechts im Toolbar `#todo-ask-bar` → Antwort einklappbar `#todo-ask-answer`, `todo.js` `_ask()`): freie Frage über den (aktiven) Bestand inkl. **Personen-/Kollegen-Auswertungen** (Profile aus Aufgaben/Notizen/Status/Fristen); serialisiert `todo_graph_data`+`todo_export` (Anhang-`md_text`), **lokal-bevorzugt** `_analysis_model` (respektiert Geheim-Modus/`confidential_allow_api`, 503 ohne lokales LLM), Einzelabfrage **oder Map-Reduce** bei großem Bestand (Budget `int(_profile_num_ctx()*3.2)`, Vorlage `/api/pst/ask`+`/api/pst/summarize`), Systemprompt `_TODO_ASK_SYSTEM` (nur aus den Daten, bei Personen sachlich-neutral). **Robustheit** (lokale Modelle sind langsam): Arbeitsmenge auf `MAX_GROUPS=6` Map-Läufe gedeckelt (überzählige Bereiche werden mit Hinweis weggelassen → lieber Einzelprojekt aktivieren); LLM-Fehler werden als klare HTTP-Meldung zurückgegeben statt als nacktes 500 (`httpx.ConnectError`→503 „Ollama nicht erreichbar", `HTTPStatusError`→502 „Modell abgelehnt/num_ctx/VRAM", sonst 500 mit Typ). Frontend zeigt `detail` an. **Demo-Datensatz** `defaults/todo_demo.json` (`todo_export`-Form + `version`-Feld, stabile `*_demo_*`-IDs, **~25 Projekte / ~1000 Punkte / 14 fiktive Kollegen**, prozedural erzeugt): via `_seed_todo_demo()` beim Start (nach `migrate_todo_json`) importiert, gesteuert über Config-Flag `seed_todo_demo`. Der Marker `data/todo/.demo_seeded` speichert die zuletzt eingespielte **Version** → ändert sie sich (neue Demo), wird **automatisch neu** eingespielt; dabei werden ALLE `tp_demo_*`-Projekte zuerst gelöscht (saubere Ersetzung, eigene Projekte bleiben; Kanten haben keine stabile ID). Fiktive Kollegen projektübergreifend → Wissensgraph-Hubs + Demonstration „vernetzte Informationen" (operative Projekte + Firmenfest + bewusst deplatziertes „Persönliches"). **Export/Import/Reset**: `GET /api/todo/export` (→ `todo_export()`, Frontend-Download), `POST /api/todo/import` (delete-then-`todo_import`, Wurzelzeile übersprungen, idempotent), `POST /api/todo/reset` (Auto-Backup nach `data/todo_backups/todo_backup_<ts>.json` **dann** `db.todo_wipe()` = alles außer Wurzel leeren) — Buttons `#btn-todo-export/-import/-reset` im Toolbar. **Liste-UI** (`todo.js`): vertikaler Splitter `#todo-tree-splitter` jetzt per `_initSplitter()` verschiebbar (Breite in localStorage `…_todo_treew`); die drei `.var-sec` sind `.todo-sec` in einer Flex-Spalte (`#todo-view-liste` = flex) mit **einklappbaren** Köpfen (`.todo-sec-toggle`) und **ziehbaren** `.todo-hsplit`-Trennern (`_initVStack`/`_startVDrag`, Zustand in localStorage `…_todo_layout`). **Punkte-Filter** inline in der Kopfzeile (`_applyItemFilters`): `#todo-filter-done` (erledigte aus), Datumsbereich `#todo-filter-from/-to` (`_parseDue` normalisiert ISO/DE), Zuständige `#todo-filter-assign`; Graph-**Personenfilter** `#todo-graph-person` (`_populateGraphPerson`, filtert Items im `_buildGraph`). **Graph-Schutz**: `_buildGraph` bricht bei > `LIMIT`(300) Punkten mit Warnung ab (Cytoscape-Layout auf ~1000 Knoten friert den Browser ein) und rät zu Einzelprojekt (⚡)/Personenfilter; „Trotzdem aufbauen" → `_buildGraphForce`. **Agenda** = deterministische Empfehlung „was als Nächstes" (Fälligkeit via `_todo_parse_due` + entblockt/blockiert aus `todo_edges` + Status; Buckets jetzt/demnächst/blockiert; Untertab „🎯 Empfehlung", Personenfilter). **Wissensgraph** (Cytoscape, Hub-Muster wie Matrix): Knoten=Punkte, Hubs=Zuständige+Status, Kanten=Verknüpfungen; mehrere Projekte → namespaced `proj::id`, Farbe je Projekt, geteilte Zuständigen-Hubs verbinden. **Backup:** `todo_export()`→`todo/todos.json` + `data/todo_att/` (Regel: DB-Bereiche werden als JSON exportiert, nicht die `.db`). `_todo_save_project` nutzt **Diff-Upsert** (Anlagen bleiben bei Edits erhalten). Modellrolle `general`; Token-Label „To-Do". **CSS**: `#todo-graph` braucht feste Höhe (sonst 0px → unsichtbar). **3D-Kugel-Wissensgraph** (`todo.js` internes `_graph3d`-Modul, Umschalter `#btn-todo-graph-2d`/`-3d`, `_graphMode` in localStorage): eigenes leichtes **Canvas-3D ohne Fremd-Dependency** (keine three.js/WebGL-Lib — self-contained/offline/MIT); Knoten aus demselben `_graphElements` auf einer **Fibonacci-Kugel** (Hubs nach innen), Perspektiv-Projektion + Tiefensortierung, Drag=drehen/Wheel=zoom/Klick=Punktdetails, **rAF-Schleife → nicht blockierend** (verträgt ~1500 Knoten, eigener Guard `LIMIT3D`; Schleife stoppt außerhalb des Graph-Views). **Anti-Freeze** („Seite reagiert nicht"): das 2D-Cytoscape-`cose`-Layout läuft jetzt **asynchron** (`animate:true`+`layoutstop`) statt synchron, plus die 3D-Kugel als flüssige Alternative für große Bestände (2D-Guard nennt sie). Canvas `#todo-graph3d` braucht feste Höhe (`#todo-graph3d-wrap`). **Phase 2 offen:** erweiterte Extraktion (Tätigkeit/Ressourcen/Fragen/Start/Ziel/Dauer) + `to-plan`-Übergabe an den Planer | ENTWICKLUNG §21.2 (3D-Kugel/Anti-Freeze) |
| Chat → Tab-Übernahme (Menü „↪ senden an…") | Tab-übergreifendes Handling: jede Assistenten-Antwort hat in der `msg-save-bar` ein Menü **„↪ senden an…"** (`_openHandoffMenu`) mit Zielen aus `_HANDOFF_TARGETS` (key=`data-tab`). `_handoffToTab(key, md)` routet: **To-Do** (`_openTodoHandoff`: Rückfrage neu/ergänzen, `_parseListItems` → `POST /api/todo/projects`+`PUT …/{id}` bzw. GET→merge→PUT, dann `Todo.refresh()`), **Planer** (`_openPlanHandoff`: Plan-Name → `Planner.openFromText` = Dokument→Plan), **Code** (`CodeIDE.loadFromChat`, Tab `ide`), **Mathe/Medizin/Varianten/Morph/Patente/Anfrage** (füllt das Haupteingabefeld vor: `mathe-input`/`medizin-input`/`var-problem`/`morph-problem`/`pat-search-term`/`rfq-chat-input` + `switchTab`), **Rechnung/Zeugnis** (formularbasiert → Text in die Zwischenablage + `switchTab`). **Auto-Angebot** per Merker `_tabHandoffPending`, vor dem Stream via `_todoIntent`/`_planIntent`/`_tabIntent` gesetzt (letzterer erkennt „<Tab>-Tab"), nach der Antwort an `_handoffToTab` geroutet. Rein Frontend, modell-agnostisch, nutzt vorhandene Endpoints/Module-APIs | — |
| Transkription (Audio → Text) + Geheim-Modus | Tab „🎙 Transkription" (`data-tab="transcription"`), `transcription.js` (`Transcription.init/refresh`), **`tools/transcribe.py`** (weicher `faster_whisper`-Import, pure logic). Quelle Mikrofon (getUserMedia+MediaRecorder, USB-Geräteauswahl via `enumerateDevices`) oder Datei; Engine **lokal** (faster-whisper, CPU/int8 — kein `_model_session`-Guard nötig, belegt kein Ollama-VRAM; GPU per `config.json stt_device`) oder **API** (`/audio/transcriptions`, OpenAI/Groq via `_llm.resolve`). Endpoints `POST /api/transcribe` (multipart, `engine`/`model`/`language`/`task`; Audio → `data/transcripts/`) + `GET /api/transcribe/engines`. Config: `stt_model`/`stt_device`/`stt_compute`/`stt_download_root` (Default `models/whisper`, gitignored; Installer cachen `base` vor, Portable bundelt offline). Ergebnis mit Zeitmarken, „→ Chat/RAG/To-Do"; **Chat-Diktat** `#btn-chat-mic` schreibt ins `#message-input`. **Geheim-Modus** erzwingt `engine=local`. Audio ≠ Token-Strom → kein `TokenMeter`. Lizenz: faster-whisper zieht PyAV/ffmpeg-LGPL (dokumentierte Ausnahme, siehe Project Constraints). **Backup:** Audio unter `data/transcripts/` ist in `_backup_dirs_bulk()` (Schalter „uploads"). **CSS:** `#transcription-panel`-Controls müssen `--bg-input`/`--text`/`--border` setzen (kein globales Formular-Styling → sonst browser-weiß) | not yet in ENTWICKLUNG.md |
| Sprachausgabe (TTS, Antworten vorlesen) | **`static/js/tts.js`** (`TTS.init/speak/toggle/stop/available`), rein clientseitig über die **Web Speech API** (`speechSynthesis`) — zero-dependency, plattformneutral, nichts wird gespeichert. Persona (`tone`) → Stimmenprofil (`PERSONA_VOICE`): roboter=synthetisch, professor=älterer Mann, doktor=ältere Frau, felix=junger Mann, sandra=junge Frau — Geschlecht per Namensheuristik aus den Systemstimmen, Alter/Klang über `pitch` (tief=älter) + `rate`; Werte bewusst tempo-angehoben (`rate>1.0`) und färbungsarm (pitch nah an 1.0), da die Browser-Systemstimmen sonst langsam/synthetisch wirken. 🔊-Knopf je Assistenten-Antwort (`chat.js` `msg-save-bar`), im Transkriptions-Tab (`#tr-speak`) und Test-Knopf im Profil (`#profile-tone-test`). `TTS.init()` in `app.js`; `.speaking`-CSS-Puls. **Optional API-TTS:** Profil-Feld `#profile-tts-model` (`tts_model`, `anbieter::modell` z. B. `openai::tts-1`) → `TTS.speak` ruft dann `POST /api/tts` (Backend `/audio/speech`, Persona→Stimme via `_TTS_VOICE_MAP`), spielt das mp3, **fällt bei Fehler/409 auf Browser zurück**. **Sichtbarer Hinweis** (`_warnApi`, entprellt 20 s): 409 = API nicht aktiv → still Browser; jeder andere Status (z. B. 502/404, Anbieter ohne `/audio/speech` wie OpenRouter/Groq) → `showToast` „API-Sprachausgabe fehlgeschlagen (HTTP …)", damit der Nutzer merkt, warum trotz gewähltem Modell die Browserstimme läuft. `GET /api/tts/config` baut die Auswahl (Browser + je Anbieter TTS-Vorschläge). **Geheim-Modus** erzwingt Browser (`/api/tts` → 409). Audio ≠ Token-Strom → kein `TokenMeter`. **Antwortstil-Personas** (`VALID_TONES`/`_TONE_PROMPTS`, Profil `tone` → `_tone_prompt()`/`_persona_prefix()`): roboter/professor/doktor/felix/sandra + **`hartman`** (»Gunnery Sergeant Hartman«, militärisch-zackiger Stil, sachlich korrekt/ohne Beleidigung). **Hartman ist ein Lokal-Riegel:** Helfer `_hartman()` fließt **zentral** in `_secret_local()` (⇒ alle Modelle lokal, API-Bild/-TTS gesperrt, Transkription lokal) **und** `_web_search_allowed()` (⇒ Websuche komplett aus: Chat-`web_search`-Tool entfernt, `/api/search/expand` 403-artig, Frontend `Profile.isHartman()`/`_applyPersonaLock()` sperrt den 🔍-Toggle). Der **Geheim-Button** (`local_only_mode`) bleibt dabei unangetastet | not yet in ENTWICKLUNG.md |
| Bildgenerierung (Text → Bild) | Backend `main.py` `/api/image/config` + `/api/image/generate` (Muster **wie TTS**), Frontend `static/js/chat.js`. **Zwei Wege, Profilwahl `image_model`** (Getter `_image_model()`): **lokal `local::sd`** = eigener **Stable-Diffusion-WebUI**-Server (AUTOMATIC1111/Forge, `POST {sd_url}/sdapi/v1/txt2img`, URL im Profil `sd_webui_url` via `_sd_url()`, mit `--api` starten) — vom Nutzer betrieben, keine neue Abhängigkeit, kein `_model_session`/VRAM-Guard (separater Server); **API `<anbieter>::<modell>`** = OpenAI-kompatibles `POST {base}/images/generations` (`dall-e-3`/`gpt-image-1`, `response_format:b64_json`). Größe über **Voreinstellungen** (`_IMAGE_SIZES`: square/landscape/portrait; `_api_image_size` mappt je Modellfamilie — `gpt-image-1` → 1536-Varianten). Antwort = Data-URI. **Geheim-Modus** (`_secret_local()`): API gesperrt → auf `local::sd` umgeleitet, ohne SD-URL **HTTP 409** (keine Cloud-Anfrage); `/api/image/config` blendet API-Optionen aus. Bild ≠ Token-Strom → **kein `TokenMeter`** (wie Audio). **Auslöser im Chat:** 🎨-Toolbar-Haken `#btn-image-toggle` (One-shot, nächste Nachricht = Bild-Prompt), Befehl `/bild <Beschreibung>` (`_parseBild`/`runBild`) und **geführter Dialog** `/bildhelp` (`runBildHelp`, festes Formular-Overlay `#bild-help` — Motiv/Stil/Kameraperspektive/Beleuchtung/Seitenverhältnis/Negativ-Prompt, komponiert den Prompt **deterministisch ohne LLM** → Geheim-tauglich). Profil-Modal: `#profile-image-model` + `#profile-sd-url` (`profile.js` `_fillImageSelect`, `Profile.imageModel()`). **Karte im Chat** (`insertMap`) hat jetzt eine definite Breite (`width:min(680px,calc(100vw-60px))`), da die Assistenten-Blase als schrumpfendes Flex-Item sonst am breitenlosen Leaflet-div kollabiert | not yet in ENTWICKLUNG.md |
| Z-Image-Turbo (lokales Bildmodell, eigenständig) | **Kein Backend-Bezug** — separates Kommandozeilen-Werkzeug im Ordner **`z-image/`** (`generate.py`, `bild.bat`/`bild.sh`, Installer `install_zimage.ps1`/`.bat`/`.sh`, `requirements.txt`, eigene `README.md`). Führt **Z-Image-Turbo** (Tongyi-MAI, 6B, Apache-2.0) über `diffusers` (aus dem Quellcode: `ZImagePipeline`, noch in keinem Release) **komplett lokal auf der GPU** aus; **eigene venv**, hängt NICHT am FastAPI-Backend/`_model_session`. Turbo-Defaults: `num_inference_steps=8`, `guidance_scale=0.0`, 1024². **VRAM-Schutz:** entlädt vor dem Lauf automatisch geladene Ollama-Modelle (`ollama stop`, abschaltbar `--keep-ollama`), misst freien VRAM (`torch.cuda.mem_get_info`) und weicht unter `--min-free` (Std. 18 GB) selbsttätig auf CPU-Offload aus (`--offload`/`--offload-seq`). Modellgewichte (~20 GB) landen im HF-Cache, nicht im Repo (`.gitignore`: `z-image/outputs/`, `z-image/venv/`, `z-image/hf_token.txt`). **Optionaler HF-Token** (beschleunigt Download): `HF_TOKEN`-Env, Installer-Parameter (`-HfToken`/`--hf-token`) oder lokale `hf_token.txt` (Geheimnis, gitignored, `_load_hf_token()`) — reine Zugangs-Config, keine neue Abhängigkeit (MIT-konform). Nicht mit der Chat-Bildgenerierung (`local::sd`/API) zu verwechseln — das bleibt der Weg im Framework. **Brücke `sd_server.py`** (**crash-sicher, Unterprozess je Bild**): stdlib-`http.server`, keine neue Abhängigkeit, `sd_server.bat`/`.sh`; A1111-kompatibler Server (`POST /sdapi/v1/txt2img` → `{"images":[b64]}`), **ignoriert** die vom Framework fest gesendeten `steps=28`/`cfg=6.5` und nutzt Turbo (8 Schritte, guidance 0). **Crash-Sicherheit:** der Server importiert selbst KEIN torch/CUDA; jede Erzeugung läuft in einem **frischen Unterprozess** (`generate.py`, ein Bild → beendet sich → VRAM komplett frei; Standard `--offload` = Spitze ~12 GB, `--full-gpu` schneller). So kann weder VRAM über mehrere Läufe fragmentieren (das ließ die frühere In-Prozess-Variante beim 2. Bild hart abstürzen und riss den Rechner mit) noch ein CUDA-Fault den Server/Rechner mitreißen — stürzt eine Erzeugung ab, stirbt nur der Unterprozess (HTTP 500), der Server lebt weiter. Preis: ~30–50 s Modell-Laden je Bild. `generate.py` macht die VRAM-Sicherung (Auto-`ollama stop`, Offload). Damit erzeugt die Chat-🎨 (`local::sd`) **und `[bild]`-Workflow-Schritte** lokal über Z-Image, sobald `sd_webui_url` = `http://127.0.0.1:7860` gesetzt ist | — |
| KI-Bilder in Präsentationen | Backend `main.py` `POST /api/presentation/slide-image` (Helfer `_slide_image_prompt`: Folientext → Bild-Prompt per kurzem LLM-Call, `_model_for("general")` → Geheim/Hartman lokal, deterministischer Rückfall; dann `_generate_image_core`). Frontend `canvas.js` `generateSlideImage`/`generateAllImages` (Knöpfe `#btn-slide-image`/`#btn-slides-images` in der Folien-Navigation, nur bei Präsentationen sichtbar) setzen das Ergebnis als `slide.image_right` + `slide.left` (Text) + `layout='two-column'` → **Canvas-Renderer UND PPTX-Export** (`tools/export.py`, `image_right`/`_embed_b64_image`, zusätzlich `image`-Rückfall + `elif img_r`-Zweige) zeigen es ohne neuen Zeichencode. Token-Label „Präsentationsbild". Nicht zu verwechseln mit `illustrated_presentation.js` (baut Folien **aus** einem Bilderordner via Vision) | not yet in ENTWICKLUNG.md |
| Geführter Präsentationsassistent (`/praesentation`) | Chat-Befehl `/praesentation <Thema>` (Aliase `/präsentation`,`/vortrag`,`/slides`) öffnet ein **Interview**-Overlay (`chat.js` `_openPresInterview`: Zielgruppe/Ziel/Umfang/Bilder/Bildwünsche/Web, dynamisch, kein index.html), dann SSE `POST /api/presentation/guided` (`_guided_presentation_generator`): **Gliederung** (`_pres_structure`, JSON `{title,subtitle,sections:[{title,illustrate}],closing}`) → **Inhaltsverzeichnis**-Folie → **je Punkt Webrecherche** (`search_with_sources`, Gate `_web_search_allowed`/`_research_model`) + Zusammenfassung (`_pres_section_content`, format json) → **Bilder** über `_slide_image_prompt`+`_generate_image_core`: **flächiges Deckblatt + Abschluss** (`layout:"title"` + `slide.image`, preset `landscape` → Renderer zeichnet Bild vollflächig, siehe `canvas.js` `drawTitleSlide`/`_drawCoverTitle` + `tools/export.py` `to_pptx` title-image-Zweig), Inhaltsfolien **zweispaltig** (`image_right`, square). **Bild-Entscheidung** `image_mode` smart (KI-Flag `illustrate` + `random()<0.6`)/all/none; Deckblatt+Abschluss immer (außer none). Nur Interview als Rückfrage, dann automatisch. Frames `pres_start`/`structure`/`researching`/`research_done`/`section_done`/`slide_image_start`/`slide_image_done`/`notice`/`done`. `chat.js` `runGuidedPresentation` → `CanvasRenderer.render`+`switchTab('canvas')`. Token-Label „Präsentationsassistent". (Der frühere One-Shot `/api/presentation/illustrated`+`runIllustratedPresentation` bleibt im Code, wird aber vom Befehl nicht mehr aufgerufen.) | — |
| Bildbearbeitung img2img (`/bildedit`) | Chat-Befehl `/bildedit [Anweisung]` (Aliase `/bildbearbeiten`,`/imgedit`,`/edit`): Bild-Picker → Overlay (`chat.js` `_openBildEditForm`: Anweisung + Stärke-Chips leicht/mittel/stark) → `POST /api/image/edit` (`_edit_image_core`). **Lokal** (`local::sd`): `POST {sd_url}/sdapi/v1/img2img` (`init_images`/`denoising_strength`) → Brücke `sd_server.py` startet `generate.py --init <tmp> --strength` in frischem Unterprozess (`ZImageImg2ImgPipeline`, crash-sicher). **API**: multipart `POST {base}/images/edits` (nur fähige Modelle wie `gpt-image-1`; `dall-e-3` kann es NICHT → klare 502-Meldung). Geheim/Hartman: nur lokal. Ergebnis `{image}` (Data-URI), „✏️ weiter bearbeiten" reicht das Ergebnis erneut in den Edit. **Inpainting** (nur markierten Bereich ändern): Overlay-Button „🖌 Bereich markieren" öffnet einen **Vanilla-Canvas-Pinsel** (kein Fremdlib) → Maske (schwarz/weiß, weiß=ändern) als Data-URI im Feld `mask`; lokal → `mask` im img2img-Payload → `generate.py --mask` (`ZImageInpaintPipeline`), API → Maske wird nach **Alpha-Konvention** konvertiert (weiß→transparent) und als `mask`-Datei an `/images/edits`. Bild ≠ Token-Strom → kein TokenMeter | — |
| Upscaling (`/upscale`) | Chat-Befehl `/upscale` (Aliase `/hochskalieren`,`/vergroessern`) + Knopf „🔍 hochskalieren" am `/bildedit`-Ergebnis: Bild-Picker → Overlay (`chat.js` `_openUpscaleForm`: Methode ✨KI-Detail/⚡Schnell) → `POST /api/image/upscale` (`_upscale_image_core`). **fast** = Lanczos + UnsharpMask (Pillow, kein VRAM). **ai** = Z-Image-**img2img mit niedriger Stärke 0.30** am Zielmaß über die Brücke (`{sd_url}/sdapi/v1/img2img`, init hochskaliert → Details ergänzt). Deckel lange Seite **2048** (ai)/4096 (fast), Vielfaches von 16. **Kein Absturz**: kein lokaler Server / Remote-Modell / Brücken-Fehler → Rückfall auf Lanczos (`note`). MIT (diffusers/Pillow, keine neue Dep). Bild ≠ Token-Strom → kein TokenMeter | — |
| Bild → Prompt (`/bildprompt`) | Chat-Befehl `/bildprompt [Stil]` (Aliase `/img2prompt`,`/bild2prompt`,`/imageprompt`), `POST /api/image-to-prompt` (`image_to_prompt`): Vision-Modell (`_pick_model`, `tools.imaging.downscale`, `images:[…]` wie `/api/analyze-image`) → EIN kompakter Text-zu-Bild-Prompt. Frontend `chat.js` `_parseBildPrompt`/`runBildPrompt`/`_doBildPrompt`: eigener Bild-Picker (Data-URL, self-contained), zeigt Prompt + Knöpfe „🎨 Bild daraus erzeugen"(`runBild`)/„📋 kopieren". Token-Label „Bild→Prompt" | — |
| Arbeitsablauf im Chat (`/workflow`) | `chat.js` `_parseWorkflow`/`runWorkflow`/`_workflowToPresentation` (Aliase `/ablauf`,`/flow`), SSE `POST /api/workflow` (`_workflow_generator`): nummerierte Schritte werden **nacheinander** als fokussierte Teilaufgaben ausgeführt (rein LLM, kein Werkzeug-Loop → robust auch für kleine Modelle), Zwischenergebnisse fließen als Kontext in den nächsten Schritt (Zeichenbudget an `_profile_num_ctx()` gekoppelt), am Ende **Synthese** zu einem Gesamtergebnis. Frames `workflow_start`/`step_start`/`searching`/`search_done`/`notice`/`step_done`/`synthesizing`/`text`/`done`/`error`; jeder Schritt als einklappbares `<details>` (`.wf-step`), Gesamtergebnis als Markdown. **Pro-Schritt-Modell + Websuche (Tags):** ein Schritt darf mit `[lokal]`/`[api]`/`[web]` (Kombis `[lokal,web]`) beginnen — Frontend `_wfParseTags` → Schritte als Objekte `{text,mode,web}`, Backend `_wf_normalize_step` toleriert auch nackte Strings. `mode='local'`→`_local_model`, `mode='api'`→ vom Frontend gewähltes Remote-Modell (`api_model`, `_wfApiModel`: erst Profil-Rolle mit `::`, sonst erstes Remote aus `/api/models`; im Geheim-/Hartman-Modus verworfen → `notice`+lokal). `web=true` **und** `_web_search_allowed()` ⇒ `search_with_sources(step,5)`, Quellen als Kontext in den Schritt (Anti-Halluzinations-Auflage); Hartman sperrt Web → `notice`. **Typischer Nutzen:** lokales Modell recherchiert+speichert zwischen, Ergebnisse fließen als Kontext an ein API-Modell (größeres Kontextfenster) — die **Synthese läuft bevorzugt auf `api_model`** (sonst Basismodell). **Übergabe-Buttons** (`.wf-actions`): „→ Präsentation" (`/api/presentation/from-text` → Canvas) und „→ Planer" (`Planner.openFromText(text,name)`, nutzt den `Dokument→Plan`-Weg mit dem kombinierten Text als `.txt`). Basis-Modellrolle `general` (Geheim/Hartman → lokal), Token-Label „Arbeitsablauf". **Medien-Schritte (`kind` image/voice):** ein Schritt darf statt Text ein **Bild** erzeugen (Tag `[bild]` ODER Text „generiere ein Bild von …" → `_WF_IMG_RE`; Motiv via `_wf_image_prompt`, Aufruf des gemeinsamen Kerns `_generate_image_core(preset="square")` → `generating_image`/`image`-Frame; Fehler = `notice`, Ablauf läuft weiter — **kein `_model_session`/GPU-Risiko im Backend, nutzt das eingestellte Bildmodell**) ODER als **Sprachnachricht** vorgelesen werden (Tag `[sprache]` ODER „… als Sprachnachricht/vorlesen" → `_WF_VOICE_RE`; erzeugt Text normal per LLM, dann `speak`-Frame → Frontend liest per `TTS.speak(text, tone)` vor, „🔊 nochmal"-Knopf). Erkennung in `_wf_normalize_step` (Backend) + `_wfParseTags` (Frontend, gleiche Regex/Tags), Badges 🖼/🔊 in `_wfBadge`. Bild/Sprache erzeugen **keine Chat-Tokens** | not yet in ENTWICKLUNG.md |
| Database schema | `db.py` | §4 |
| Tools package | `tools/*.py` | §5 |
| Frontend modules | `static/js/*.js` | §6 |
| Data file formats | `data/` | §7 |

## Tools (`tools/` package) — one-liners

| Module | Responsibility |
|---|---|
| `llm.py` | **Unified LLM access** (local Ollama ⇄ external OpenAI-compatible API); Ollama-shaped responses both ways |
| `search.py` | DuckDuckGo async search → sources list. **Gedrosselt** (`_ddgs_call`): globales Lock + Mindestpause (`_DDGS_MIN_INTERVAL` 5 s) + Backoff-Retry bei Ratelimit (bis 4 Versuche), sync ddgs in `asyncio.to_thread` — sonst blockt DDGS bei schnellen externen APIs (Matrix-Recherche) |
| `files.py` | Document extraction: PDF (pypdf), DOCX, XLSX, images |
| `export.py` | Generate DOCX/XLSX/PPTX/PDF/LaTeX (matplotlib mathtext for PDF formulas; beamer/article for LaTeX) |
| `engineering.py` | Pint units, SymPy solver, `plot_chart` (diskrete Datenpunkte, model-callable) + `plot_function` (Funktion, 400 Samples → glatt; nur **fähigen externen Modellen** als Tool angeboten, sonst deterministischer Fallback `_extract_plot_request`, weil kleine lokale Modelle ungültige LaTeX-Escapes erzeugen), VDI 2230 bolts |
| `materials.py` | ~40-material properties database |
| `report.py` | PDF/DOCX reports with LaTeX equations |
| `routing.py` | `route_planner` chat tool: Nominatim + OSRM → `map` frame |
| `imaging.py` | Image helpers for illustrated presentations |
| `anonymize.py` | PII redaction for Verzeichnis-Analyse (stdlib regex only) |
| `mail.py` | 🚧 read-only IMAP/POP3 (stdlib); sending is always manual |
| `mailstore.py` | Postfach-Import: `.pst` (Outlook-COM → `libpff`/LGPL → **eingebauter `pst_pure`**), `.mbox`/`.eml` (stdlib), `.msg` (`extract-msg`/BSD); einheitliche Mail-dicts + Anhang-Ablage. `pst_password_status()` = CRC-Passwort-Prüfung. `.pst` immer lesbar |
| `pst_pure.py` | **Reiner-Python-MS-PST-Leser** (MIT, ohne Fremdlib): NDB (Header→NBT/BBT→Blöcke, XBLOCK/XXBLOCK, permute-Decrypt) + LTP (HN-Heap→BTH→Property/Table-Context) → Nachrichten (Absender/Empfänger/Betreff/Datum/Text) + Anhänge; PST-CRC Passwort-Verifikation (`_pst_crc`, [MS-PST] 5.3). Nur Unicode-PST. CLI: `python -m tools.pst_pure <datei.pst>` |
| `rag.py` | RAG: cleanup, chunking, Ollama embeddings (CPU), NumPy cosine search; VRAM tiers, per-base `strictness`/`char_limit` |
| `patente.py` | Patent research: unified fetch (`fetch_patent` = EPO OPS primär + Google-Scraping-Fallback, gedrosselt + Datei-Cache), case-file management, Analyse-Pipeline (7 Stufen + Merkmalsanalyse/Claim-Chart), knowledge-graph data. Pure logic, no FastAPI/DB imports (`main.py` does HTTP + persistence) |
| `epo_ops.py` | **EPO Open Patent Services**: OAuth2-Token-Cache, Fair-Use-Drossel, tolerantes JSON-Parsing (`_find` ohne Namespace-Pfade); biblio/claims/description/family/legal + CQL-Suche (`build_cql`). Pure logic, nur httpx |
| `dokumente.py` | Document generation for Rechnungen/Angebote (deterministic `Decimal` amount math, §14-UStG fields, Markdown + DOCX builder) and Arbeitszeugnisse (LLM prompt/structure helpers; rendering via `tools/export.py`). Pure logic, no FastAPI/DB imports |
| `transcribe.py` | **Spracherkennung/Transkription** (Audio → Text). Weicher `faster_whisper`-Import (MIT); `transcribe_local()` (CPU/int8 default, Modell-Cache, Segmente mit Zeitmarken), `local_available()`/`list_local_models()`. Pure logic, keine FastAPI/DB. API-Weg (`/audio/transcriptions`) liegt in `main.py`. Bringt PyAV/ffmpeg-LGPL mit (dokumentierte Ausnahme) |

Calculations run inside `_safe_exec()` in `main.py` — a restricted `exec()` sandbox (no file I/O or network; whitelisted math/numpy/scipy/sympy).

## Dependencies

Python: FastAPI, Uvicorn, httpx, ddgs, pypdf, python-docx, openpyxl, python-pptx, Pillow, python-multipart, aiofiles, aiosqlite, SymPy, NumPy, SciPy, Pint, matplotlib (pinned in `requirements.txt`). **No frontend build step** — plain HTML/CSS/JS served by FastAPI `StaticFiles`.

## Deployment (summary)

Three variants, each with a `.bat`/`.ps1` (and Linux `.sh` where applicable) pair: `install` (standard: Python 3.12 + Ollama + venv), `make_portable` (self-contained bundle, own Ollama port `11500`), `make_server` (multi-user `0.0.0.0`; use `workers = 1` on ~6 GB VRAM — the VRAM guard doesn't coordinate across worker processes). Installers interactively pick optional tabs (`hidden_tabs_default`), API on/off (`enable_api`), and Python execution on/off (`allow_python_exec`). Default agents seed from `defaults/agents/` on first run. Full detail: `docs/ENTWICKLUNG.md` §20, `docs/SERVER.md`.
